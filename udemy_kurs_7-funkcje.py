# Importowanie  biblioteki math. Potem używając math wpisujemy funkcję której chcemy użyć, np.: math.sqrt(wartość)

import math
math.sqrt(4)

# Importowanie funkcji sqrt, ceil z biblioteki math. Potem wystarczy wpisać nazwę funkcji i podać wartość jak w przykładzie poniżej.

from math import sqrt
from math import ceil

sqrt(10)
ceil(5)

# Importowanie wszystkich funkcji z biblioteki math

from math import *

